---
title: "Prometheus Query Language"
date: 2025-04-08
---

The Prometheus Query Language (PromQL) is the main way to query metrics within Prometheus. It is a nested functional language, where data appears as nested expressions within larger expressions. The outermost expression defines the final value, while nested expressions represent values for arguments and operands.
